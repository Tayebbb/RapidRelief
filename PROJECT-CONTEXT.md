# RapidRelief — PROJECT CONTEXT (Single Source of Truth)

> **MANDATORY for every AI agent (Copilot, Claude, Antigravity, etc.) and every human:**
>
> 1. **READ this file completely BEFORE implementing anything.**
> 2. Read the feature's section in [RapidRelief-Development-Plan.md](RapidRelief-Development-Plan.md) before touching that feature.
> 3. **UPDATE this file AFTER every merged change** (status board row + changelog entry).
> 4. Never violate the Architecture Rules below — they are what keep 4 developers unblocked.

Last updated: 2026-09-02 · Current phase: **P1 — F0 complete; F1 DONE (full auth vertical: server + client); F8 DONE (full AI pipeline: persistence + worker + provider chain + real Gemini HTTP, opt-in via key; rule-based fallback permanent). Contracts v1 workshop ratification pending (team action).**

---

## 1. Project Snapshot

- **Product:** RapidRelief — AI Smart Disaster Response & Emergency Management System (semester project, 4 devs).
- **Core loop:** Citizen reports disaster → AI classifies/scores → Rescue teams run missions → Admin command center monitors/verifies → Relief requested, allocated, delivered.
- **Stack (frozen, see D-001):** ASP.NET Core 8 Web API (modular monolith, vertical slices) · Blazor WASM PWA (hosted) · PostgreSQL + EF Core 8 · SignalR · Leaflet/OpenStreetMap · Gemini free tier behind `IAiAnalysisService` with rule-based fallback · xUnit · GitHub Actions.
- **Team & lanes:** Tayeb (foundation/auth/AI/realtime) · Shehab (incident lifecycle: reporting/rescue/offline) · Tanjim (shelters/command center/analytics/audit) · Mugdho (relief/resources/registry/alerts).
- **Full plan:** [RapidRelief-Development-Plan.md](RapidRelief-Development-Plan.md) — feature details, effort, phases, demo script.

## 2. Repository State

| Area                                                                                                    | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Planning                                                                                                | ✅ Complete (plan + this context file + [docs/architecture/F0-blueprint.md](docs/architecture/F0-blueprint.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Solution scaffold (`RapidRelief.sln`, 3 src + 2 test projects)                                          | ✅ Created (F0 chunk 1) — builds green from fresh clone with .NET 8 SDK only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Contracts v1 (`RapidRelief.Shared/Contracts`)                                                           | ✅ Authored frozen per F0-blueprint B2 — pending team workshop ratification                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| DB pattern (per-context EF Core + Npgsql, `feature_` prefix, own history table, degraded startup D-005) | ✅ Proven by `SampleDbContext` + Initial migration + `MigrationRunner`/`DatabaseHealth` (F0 chunk 2); **`AuthDbContext` live (F1 chunk A)**; **`AiDbContext` live (F8 chunk 1)** — three contexts confirm the one-line-per-context scale-out                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| docker-compose + `.env.example`                                                                         | ✅ Per blueprint B7 (F0 chunk 2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Stubs & seed data                                                                                       | ✅ `DhakaSeedData` (28 incidents/8 shelters/6 hospitals/10 volunteers/5 NGOs/6 teams) + 4 fakes + rule-based AI + no-op notifier + local disk storage, all DI-resolvable via stub-yield `TryAdd*` (F0 chunk 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Docs & repo hygiene                                                                                     | ✅ `docs/api-conventions.md`, `docs/event-bus.md`, `.github/CODEOWNERS` (placeholder handles), `.github/pull_request_template.md` (DoD checklist) (F0 chunk 3)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| CI pipeline                                                                                             | ✅ build-test + postgres-fidelity jobs (`.github/workflows/ci.yml`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Any feature code                                                                                        | Foundation (`/api/foundation/whoami`, `/health` with DB state, `/api/foundation/demo-incidents` demo feed) + full Sample slice (`/api/sample/pings`, `/sample` page with RapidMap) + Ai/Realtime/Stubs modules + **full Auth vertical (F1: `/api/auth/*` — 11 endpoints; client `/login` `/register` `/profile` pages, JWT session provider, Bearer handler chain, silent boot refresh)** + **F8 complete (AI pipeline: `ai_assessments` persistence, IncidentCreated→worker→IncidentAssessed, duplicate detection, Gemini-with-fallback composite with REAL Gemini HTTP transport — opt-in via `Ai:Gemini:ApiKey`, golden-tested request bodies, D-024 photo inline, `/api/ai/*` endpoints)** |

**Next action for the team:** hold the contract workshop to ratify Contracts v1 (§6); all 4 devs run the app locally (F0 DoD team action); start F1/F2/F3/F4 in parallel against the stubs.

## 3. Feature Status Board

Statuses: `NOT STARTED` → `IN PROGRESS` → `MVP DONE` → `DONE` (· `BLOCKED` should never appear — if it does, fix the process, see plan §1.5).

| ID  | Feature                                   | Owner  | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | ----------------------------------------- | ------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F0  | Platform Foundation & Shared Kernel       | Tayeb  | DONE        | All 3 chunks shipped: Contracts v1 + event bus + module system + MultiAuth/FakeAuth + EF per-context pattern + degraded mode + Sample slice + deterministic Dhaka stubs behind all 7 contract interfaces (stub-yield TryAdd) + rule-based AI + no-op notifier + local disk storage + vendored Leaflet/RapidMap (seeded markers on `/sample` via demo-incidents feed) + DevRolePicker (X-Dev-Role handler) + docs/CODEOWNERS/PR template + CI — 77/77 tests green, build/publish 0 warnings, no-DB degraded smoke verified. Workshop ratification of Contracts v1 = pending team action. **Post-review hardening applied** (2026-09-01: 23-item code+security review batch, 94/94 tests)                                          |
| F1  | Authentication, Profiles & RBAC           | Tayeb  | DONE        | Full auth vertical works: server slice (chunk A — Identity Core, `AuthDbContext`, JWT 30 min + rotating 7 d refresh cookie w/ reuse→family revoke, 11 endpoints, `IdentityUserAdminService`, seeder) + client slice (chunk B — `JwtAuthStateProvider` in-memory session, `AuthMessageHandler` Bearer/X-Dev-Role-strip + single-flight refresh, handler-free `AuthApi`, `/login` `/register` `/profile` pages, `AuthorizeRouteView` + RedirectToLogin, LoginDisplay, DevRolePicker disabled while signed in, silent boot refresh). FakeAuth/DevRolePicker coexist untouched (signed-out flow). Workshop ratification pending                                                                                                      |
| F2  | Disaster Reporting & SOS                  | Shehab | NOT STARTED | Owns Incident aggregate + state machine. **F2 ingestion must cap description length (AI carry-out)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| F3  | Shelter Management & Finder               | Tanjim | In progress |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F4  | Relief Requests & Tracking                | Mugdho | NOT STARTED |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F5  | Rescue Team Operations                    | Shehab | NOT STARTED | Queue must work without AI scores (fallback sort)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| F6  | Mission Assignment & Team Registry        | Shehab | NOT STARTED | Manual assign = MVP; AI recommend = advanced                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| F7  | Admin Command Center & Verification       | Tanjim | NOT STARTED | Build against fake read services first                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| F8  | AI Analysis Engine                        | Tayeb  | DONE        | Full pipeline live: chunk 1 (offline pipeline/persistence/duplicates/endpoints) + chunk 2 (real `GeminiClient` HTTP via named client + key header + D-026 linked-CTS timeouts + zero retries; `GeminiPromptBuilder` golden-tested — verbatim systemInstruction/responseJsonSchema, closing-tag escaping, no PII in payload; D-024 first-photo inline with text-only degradation; live smoke behind `GEMINI_API_KEY`). Gemini is opt-in via key — rule-based fallback permanent (§4.5). **Post-review hardening applied (2026-09-02: probe-leak fix, multi-part parser, 4000-char description cap, duplicate-scan index).** **Pre-demo Wk-10 task: 5-min live latency/quota check per D-023; drop model via config if p95 > 5 s** |
| F9  | Real-Time Hub & Notification Center       | Tayeb  | NOT STARTED | Consumers keep polling fallback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F10 | Emergency Broadcast Alerts                | Mugdho | NOT STARTED | Works pre-F9 via polling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F11 | Resource Inventory, Allocation & Delivery | Mugdho | NOT STARTED | Consumes own F4                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F12 | Analytics, Heatmaps & Response Metrics    | Tanjim | NOT STARTED | Read-only via contracts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| F13 | Hospital, Volunteer & NGO Registry        | Mugdho | NOT STARTED |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| F14 | Audit Trail                               | Tanjim | NOT STARTED | Pure event subscriber                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F15 | Offline Reporting & Auto-Sync (PWA)       | Shehab | NOT STARTED | Wow feature; needs F2 idempotency key                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| F16 | AI Emergency Assistant                    | Tayeb  | NOT STARTED | Reuses F8 provider chain                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| F17 | Safety Zones & Road Closures              | Tanjim | NOT STARTED | Stretch only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

## 4. Architecture Rules (NEVER violate)

1. **Vertical slices:** all code for a feature lives in `Features/<Feature>/` (Api + Client). A feature folder may reference `Shared/Contracts` and itself — **never another feature's folder**.
2. **Contracts are the only cross-module surface.** Cross-module reads go through contract interfaces (`IIncidentReadService`, `IShelterReadService`, …); cross-module writes happen only via events (`IncidentCreated`, `MissionStatusChanged`, …).
3. **No cross-module foreign keys or EF navigation properties.** Reference other modules by plain `Guid` ID.
4. **Per-owner DbContexts** with separate migration histories: `AuthDbContext`, `IncidentsDbContext`, `OpsDbContext`, `ReliefDbContext`, `AiDbContext`. Never add your tables to someone else's context. Never edit a merged migration — add a new one.
5. **Stubs stay alive.** Real implementations replace fakes via DI only; `FakeAuthHandler` (Development-only, header `X-Dev-Role`), rule-based AI, no-op notifier + polling fallbacks must keep working for the whole semester — they are demo resilience, not scaffolding.
6. **Contract changes are additive.** New optional fields OK; renames/removals/breaking changes require a `contracts`-labeled PR with 2 approvals. Never break Contracts v1 silently.
7. **Lane ownership:** only edit `Features/X` folders you own (or the task explicitly assigns). Shared hot spots: each feature self-registers via its own `{Feature}Module.cs` — never edit another feature's module file.
8. **Every external service sits behind an interface** (`IAiAnalysisService`, `IFileStorage`, `IRealtimeNotifier`) with a working fallback. The demo must never depend on network/quota.
9. **Security non-negotiables:** validate all input (FluentValidation), authorize by role policy on every endpoint, validate uploads (type/size), rate-limit auth + report endpoints, user text is data — never AI instructions.

## 5. Conventions Quick Reference

- **Routes:** `/api/{feature}/...` · standard response envelope + ProblemDetails errors (see plan §8.9).
- **Branches:** `feat/f{NN}-{short-name}`, `fix/f{NN}-{short-name}` — short-lived (≤3 days), PR into `main`, CI green + 1 approval (2 for contracts).
- **Commits:** Conventional Commits — `feat(reporting): ...`, `fix(shelters): ...`, `test(ai): ...`.
- **Tables:** prefixed `feature_tablename`.
- **Seeded users (dev):** `citizen1@rr.dev` / `rescue1@rr.dev` / `admin1@rr.dev` / `ngo1@rr.dev` — password `Demo!123` (Development only; never in production config).

## 6. Contracts v1 Registry

> Status: **FROZEN per [docs/architecture/F0-blueprint.md](docs/architecture/F0-blueprint.md) §B2 — pending workshop ratification.** Authored in `RapidRelief.Shared/Contracts` (F0 chunk 1); exact signatures live in code and blueprint B2. Changes are additive-only (§4.6) — `contracts`-labeled PR + 2 approvals.

- **Common:** `GeoPoint`, `PagedResult<T>`, `ApiEnvelope<T>`
- **Enums:** `DisasterType`, `Severity`, `IncidentStatus`, `MissionStatus`, `ReliefStatus`, `ResourceType`, `Roles`
- **Eventing:** `IEvent`, `EventBase`, `IEventHandler<T>`, `IEventBus`
- **Events:** `IncidentCreated`, `IncidentAssessed`, `IncidentVerified`, `MissionAssigned`, `MissionStatusChanged`, `ReliefRequested`, `ReliefStatusChanged`, `AlertPublished`, `AuthEvent`, `PingCreated`
- **Read models:** `IncidentSummaryDto`, `IncidentQuery`, `ShelterSummaryDto`, `HospitalSummaryDto`, `VolunteerSummaryDto`, `NgoSummaryDto`, `UserSummaryDto`, `AiAnalysisRequest`, `AiAssessmentDto`, `StoredFile`
- **Interfaces:** `IIncidentReadService`, `IShelterReadService`, `IRegistryReadService`, `IUserAdminService`, `IAiAnalysisService`, `IRealtimeNotifier`, `IFileStorage`

## 7. Decisions Log (append-only)

| ID    | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Why                                                                                                                                                                                                                                                                                                          |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --- | ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| D-001 | 2026-09-01 | Stack: ASP.NET Core 8 modular monolith + Blazor WASM PWA + PostgreSQL + SignalR + Leaflet/OSM + Gemini-with-fallback                                                                                                                                                                                                                                                                                                                                                                                    | One language for 4 devs, $0 cost, offline + realtime supported, demo cannot fail on quota (plan §1.1)                                                                                                                                                                                                        |
| D-002 | 2026-09-01 | Zero-blocking model: contracts-first + stubs + fake read services + per-owner DbContexts + no cross-module FKs                                                                                                                                                                                                                                                                                                                                                                                          | Hard requirement: no developer ever waits on another (plan §1.5)                                                                                                                                                                                                                                             |
| D-003 | 2026-09-01 | This file is the single source of truth for implementation state; all agents read before / update after any change                                                                                                                                                                                                                                                                                                                                                                                      | Keeps multi-agent, multi-dev work consistent                                                                                                                                                                                                                                                                 |
| D-004 | 2026-09-01 | Stay on .NET 8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | D-001 froze the stack; all package pins are validated for 8.x; EOL (2026-11-10) is irrelevant for a graded local demo ending ~Week 13, while migrating means 4 SDK installs + redoing all research under deadline                                                                                            |
| D-005 | 2026-09-01 | docker-compose is the documented primary DB path; Neon/Supabase free tier is the documented fallback (override via `ConnectionStrings__Postgres` env var or user-secrets); startup is warn-and-continue-degraded — `MigrateAsync` retries 3× then logs a prominent warning, sets `DatabaseHealth.PostgresAvailable=false`, app keeps serving (stub-backed pages work; DB-backed endpoints return 503 ProblemDetails; `/health` reports it)                                                              | Consistent with rule §4.8 ("demo must never depend on network"); a dev with no Docker/Postgres still runs the app against stubs, and all F0 tests are provable via the SQLite factory alone                                                                                                                  |
| D-006 | 2026-09-01 | Hand-rolled in-process event bus (~50 lines) instead of MediatR notifications (deviation from plan §8.6)                                                                                                                                                                                                                                                                                                                                                                                                | MediatR is commercial from v13 (accidental `dotnet add package` = license risk for students); we need only pub/sub notifications, and a zero-dependency bus with per-handler try/catch gives exactly the plan's "missing subscriber breaks nothing" semantics                                                |
| D-007 | 2026-09-01 | F0 ships the per-context infrastructure pattern + exactly ONE concrete context (`SampleDbContext`); `AuthDbContext`/`IncidentsDbContext`/`OpsDbContext`/`ReliefDbContext`/`AiDbContext` arrive with their owning features copying the proven pattern; consequently ASP.NET Identity + seeded Identity users defer to F1's first PR (FakeAuth covers all 4 roles until then)                                                                                                                             | Empty contexts are ceremony that forces Tayeb to scaffold inside teammates' lanes (violates §4.7); one real context proves history-table naming, `feature_` prefix, `--context`/`--output-dir` usage, and startup migration orchestration — everything owners need to copy                                   |
| D-008 | 2026-09-01 | Sample slice = `Features/Sample` "Ping": `POST /api/sample/pings` (Admin policy, FluentValidation) persists `Ping` to `sample_pings` via `SampleDbContext`, publishes `PingCreated` contract event consumed by a logging handler in the same slice; `GET /api/sample/pings` (anonymous, paged envelope); Blazor page `/sample` posts+lists via the dev-role header; full integration test via SQLite factory                                                                                            | One tiny slice exercises every foundation mechanism (module self-registration, per-context migrations, envelope, validation, auth policy + FakeAuth, event bus, client page, test factory) — the literal copy-me template plan §8.1 demands                                                                  |
| D-009 | 2026-09-01 | `RuleBasedAiAnalysisService` derives its baseline severity from the predicted disaster type (collapse/quake/cyclone→Severe, flood/fire/landslide→Moderate, other→Minor) instead of blueprint B4's "severity = reported + 1", and takes an injected `TimeProvider` (registered `TryAddSingleton(TimeProvider.System)` in `AiModule`) for the recency bonus                                                                                                                                               | The frozen `AiAnalysisRequest` contract (B2) carries no reported-severity field, so B4's literal rule is unimplementable without a contract change (forbidden pre-workshop, §4.6); the type-derived baseline keeps the function pure/deterministic, and the injected clock makes "now" pinnable in tests     |     | D-010 | 2026-09-01 | TLS terminates at the app: `UseHttpsRedirection` + `UseHsts` active outside Development/Testing; reverse-proxy deploys must configure forwarded headers — see D-011 | Hosted deploys get a secure-by-default posture with zero extra config, while local dev/demo keeps plain HTTP |
| D-011 | 2026-09-01 | Forwarded headers are opt-in config (`Proxy:Enabled`; `Proxy:KnownProxies` clears default trust only when explicitly listed), registered before the rate limiter. Rate limiting partitions per-IP and REQUIRES forwarded-headers config behind proxies. `RequireRateLimiting("auth")`/`("reports")` is mandatory on F1 login + F2 report endpoints                                                                                                                                                      | Blind `UseForwardedHeaders` trusts spoofable client headers; without it behind a proxy, every client shares the proxy's IP partition and per-IP limiting is useless                                                                                                                                          |
| D-012 | 2026-09-01 | **Refresh cookie contract:** name `rr_refresh`, `HttpOnly`, `SameSite=Strict`, `Path=/api/auth`, `Secure` gated on `!IsDevelopment() && !IsEnvironment("Testing")`, `Expires` = the refresh row's `ExpiresAtUtc` (≤7d). CSRF posture = Strict cookie + JSON-body/Bearer endpoints; no antiforgery infrastructure                                                                                                                                                                                        | `Path=/api/auth` lets `/logout` see the cookie (no raw token in body); Testing joins the Secure gate because `CookieContainer` silently drops `Secure` cookies over the factory's `http://localhost` (same condition as the D-010 HSTS gate); Dev stays plain-HTTP per D-010                                 |
| D-013 | 2026-09-01 | **Token lifetimes:** access JWT **30 min** (`Jwt:AccessTokenMinutes`), refresh **7 days absolute** (`Jwt:RefreshTokenDays`) — rotation **inherits** `ExpiresAtUtc` from the replaced row (no sliding); JwtBearer `ClockSkew = 1 min`                                                                                                                                                                                                                                                                    | Issuer and validator share one clock, so the 5-min default skew only stretches the TTL; inheritance makes "absolute" true in one line                                                                                                                                                                        |
| D-014 | 2026-09-01 | **Server-side invalidation:** lock and role-change do both `UpdateSecurityStampAsync` and immediate revocation of all active refresh rows; refresh validates `SecurityStampAtIssue` against the live stamp; **reuse of a revoked token revokes the whole family** and publishes `AuthEvent("TokenReuse")`                                                                                                                                                                                               | Lock/role change becomes effective at next refresh (worst case = 30-min access TTL); stamp check catches any revocation path missed; family revocation is the standard stolen-token response                                                                                                                 |
| D-015 | 2026-09-01 | **Profile photo read path:** authenticated `GET /api/auth/profile/photo` streaming the caller's own photo via `IFileStorage.OpenReadAsync`. The F0-blueprint "public upload serving" decision stays deferred to F2                                                                                                                                                                                                                                                                                      | Zero new static-file surface; reuses hardened `LocalDiskFileStorage` traversal-safe read; photos are private-by-default                                                                                                                                                                                      |
| D-016 | 2026-09-01 | **Registration policy:** `/register` is Citizen self-serve only — server hard-assigns `Citizen`, request carries no role field; NGO/Rescue/Admin accounts are provisioned by Admin promote via `PUT /api/auth/users/{id}/roles` (register-then-promote). F13's "NGO/volunteer self-registration" = a future additive surface owned by F13, not built in F1                                                                                                                                              | Kills the one-line privilege-escalation risk; standard practice for privileged roles; keeps F13's option open without contract changes                                                                                                                                                                       |
| D-017 | 2026-09-01 | **Fixed role GUIDs**, seeded in all environments: Citizen `aaaaaaaa-0000-0000-0000-000000000001`, Rescue `…0002`, Admin `…0003`, NGO `…0004` (constant map `AuthSeeder.RoleIds`, ordinal-matching `FakeAuthHandler.SeedUserIds`); demo users only in Development/Testing                                                                                                                                                                                                                                | Deterministic role IDs across dev/CI/prod DBs simplify debugging, fixtures, and any future cross-env data move; costs nothing                                                                                                                                                                                |
| D-018 | 2026-09-01 | **PasswordHasher `IterationCount` = 210,000** (OWASP for PBKDF2-HMAC-SHA512), config knob `Auth:PasswordHasherIterations`; `TestingWebAppFactory` overrides to 10,000 for suite speed                                                                                                                                                                                                                                                                                                                   | .NET 8 default is 100k; the V3 hash format embeds the per-hash count, so mixed counts verify correctly and old hashes auto-flag `SuccessRehashNeeded` — the override is safe                                                                                                                                 |
| D-019 | 2026-09-01 | **Auth wire DTOs are feature-local, NOT contracts:** server records in `RapidRelief.Api.Features.Auth.Endpoints`, hand-mirrored client records in `RapidRelief.Client.Features.Auth` (chunk B). `Shared/Contracts` is untouched by F1 (existing `UserSummaryDto`/`AuthEvent`/`IUserAdminService` consumed as-is)                                                                                                                                                                                        | These DTOs are F1's own client↔server wire shapes, not cross-module surfaces (§6 registry definition); duplication is the price of the contract freeze (§4.6)                                                                                                                                                |
| D-020 | 2026-09-01 | Accepted: stateless JWT means lock/role-change takes ≤ TTL(30 min)+skew(1 min) to affect live access tokens; immediate on refresh path (D-014). Revisit per-request stamp checks only if ops require instant lockout                                                                                                                                                                                                                                                                                    | Per-request stamp validation would add a DB hit to every authenticated call — disproportionate for the demo threat model; the refresh path already enforces revocation immediately                                                                                                                           |
| D-021 | 2026-09-01 | **F8 async pipeline:** `IEventHandler<IncidentCreated>` enqueues to a bounded `Channel<AiWorkItem>` (DropWrite + error log when full, capacity `Ai:Pipeline:ChannelCapacity` = 100); `AiAnalysisWorker : BackgroundService` dequeues, one scope per item, runs the provider chain, persists, publishes `IncidentAssessed`. One code path — rule-based also runs through the worker                                                                                                                      | The bus runs handlers inline in the publisher's request; inline Gemini would add 1–20 s to F2's report POST. DoD says "within seconds", not "in the response"                                                                                                                                                |
| D-022 | 2026-09-01 | **F8 duplicate detection** runs against F8's own `ai_assessments` snapshot columns (lat/lon/type/reportedAt copied from the event): Haversine ≤ 300 m ∧ same declared type ∧ \|Δt\| ≤ 30 min ∧ not self; nearest wins; current-status re-check via `IIncidentReadService.GetByIdAsync` disqualifies only Resolved/Rejected, `null` keeps the candidate. Constants in code, no config knobs                                                                                                              | Frozen `IncidentQuery` has no geo/time filters; a self-contained snapshot table works identically under fake or real F2                                                                                                                                                                                      |
| D-023 | 2026-09-01 | **Gemini model pin:** `Ai:Gemini:Model` = `gemini-3.7-flash`, `thinkingLevel MINIMAL`, `temperature 0`, `maxOutputTokens 256`. Pre-demo Wk-10 task: 5-min live latency/quota check; drop to `gemini-3.5-flash-lite` via config if p95 > 5 s                                                                                                                                                                                                                                                             | Gemini 3 thinks by default (latency killer); config-only swap = zero code risk                                                                                                                                                                                                                               |
| D-024 | 2026-09-01 | **Photos to Gemini:** first photo only via `IFileStorage.OpenReadAsync` (≤10 MiB → base64 fits the 20 MB inline cap); extra photos dropped + count logged; unreadable/unknown-extension photo → proceed text-only, never fail the pipeline (lands with chunk 2's real client)                                                                                                                                                                                                                           | Two 10 MiB photos physically don't fit one request; Files API = round-trip + state for zero demo value                                                                                                                                                                                                       |
| D-025 | 2026-09-01 | **Gemini breaker:** singleton `GeminiCircuitBreaker`, TimeProvider-injected; any Gemini-path failure counts consecutively, success resets; 3 fails → open 2 min → half-open single probe (success closes, failure reopens). Config `Ai:Gemini:BreakerFailures`/`BreakerOpenMinutes`. Only Gemini-path failures count — never DB/pipeline errors                                                                                                                                                         | ~70 lines, unit-testable with a pinned clock; zero packages (Polly rejected per D-006 spirit)                                                                                                                                                                                                                |
| D-026 | 2026-09-01 | **Gemini timeouts:** 10 s text / 20 s vision (config), enforced per-request via linked CTS with `HttpClient.Timeout = InfiniteTimeSpan` (named client "gemini" registered now; real HTTP in chunk 2). Zero retries — fallback is instant and free                                                                                                                                                                                                                                                       | Retrying only delays the demo; a 429 is just a breaker-counted failure                                                                                                                                                                                                                                       |
| D-027 | 2026-09-01 | **Recommendation sources v1 (contract-only):** shelter = `IShelterReadService.GetNearestAsync` filtered `IsOpen && Occupancy<Capacity` top 3; team = available volunteers with per-type skill match via `IRegistryReadService.GetVolunteersAsync`, Haversine top 3, `sourcedFrom:"VolunteerRegistry"`; resource = NGOs by per-type focus-area match, seed order top 3, `sourcedFrom:"NgoRegistry"`. When team/resource contracts are ratified additively, only the source swaps — response shape frozen | No team/resource contract exists and `Features/Ai` cannot reference `Features/Stubs` (§4.1); `sourcedFrom` makes the proxy honest                                                                                                                                                                            |
| D-028 | 2026-09-01 | **F8 DI composition:** `AiModule` registers `RuleBasedAiAnalysisService` concretely (permanent) + `IAiAnalysisService → GeminiAiAnalysisService` composite (displaces the direct binding; DI smoke pin updated). Composite short-circuits to rule-based when `Ai:Gemini:ApiKey` is empty — missing key never crashes and never counts against the breaker. Degraded DB: worker still analyzes and publishes `IncidentAssessed`, skips persist, logs; GET stays 503/404                                  | Keeps §4.5/§4.8: fallback alive forever, demo never depends on network or DB                                                                                                                                                                                                                                 |
| D-029 | 2026-09-01 | **TestingWebAppFactory SQLite rebind:** per-context DBs switched from a single kept-open `:memory:` connection to a uniquely-named `Mode=Memory;Cache=Shared;Pooling=False` data source with a kept-open anchor connection — every scope now opens its own connection                                                                                                                                                                                                                                   | A single shared `SqliteConnection` is not thread-safe; F1's parallel-rotation test racing F8's background worker corrupted its internal command list (~50% dispose-time NRE in `SqliteConnection.RemoveCommand`). Named shared-cache keeps the exact same lifetime semantics with real per-scope connections |
| D-030 | 2026-09-02 | **`IGeminiClient` reshaped for chunk 2:** `GenerateContentAsync(string requestBody, bool isVision, ct)` replaces chunk 1's `(AiAnalysisRequest, ct)` — the client is transport-only; the composite loads the first photo (D-024, `IFileStorage` injected there) and builds the body via `GeminiPromptBuilder` before calling it                                                                                                                                                                         | Matches the blueprint chain order (load photo → build request → client) and keeps the golden-tested body construction independently unit-testable; feature-local seam, NOT a contract — no freeze impact                                                                                                     |
| D-031 | 2026-09-02 | **Golden files live in the test source tree** (`tests/RapidRelief.Api.Tests/Ai/Goldens/`), read via compile-time `[CallerFilePath]`; `UPDATE_GOLDENS=1` rewrites them from actual output; the vision golden normalizes the base64 payload to `<BASE64_PHOTO>` with prefix/length asserted separately                                                                                                                                                                                                    | No csproj copy-to-output wiring needed and byte-exact comparison stays stable; independent verbatim asserts (systemInstruction, schema, generationConfig) prevent "regenerate to hide a regression" abuse                                                                                                    |

## 8. Changelog (append-only, newest first)

- **2026-09-02** — **F8 post-review hardening** (consolidated code+security review batch; TDD RED 6 failing → GREEN): breaker `AbandonProbe()` — caller cancellation escaping between `TryEnter` and `Record*` (photo load or client call) releases the half-open probe instead of wedging the breaker forever; parser concatenates ALL string `text` parts of candidates[0] (multi-part responses) and sanitizes finishReason (strip to `[A-Za-z0-9_]`, clamp 32) before embedding in reject reasons; prompt builder caps description at 4000 chars before fencing (token-cost/oversize amplification — F2 ingestion cap = carry-out); worker publishes `IncidentAssessed` with `CancellationToken.None` after successful persist (event must survive shutdown in the gap); `(SnapshotType, SnapshotReportedAtUtc)` duplicate-scan index + `DuplicateScanIndex` migration (AiDbContext = 2 migrations, Initial untouched); golden self-heal now fails loudly after rewrite and refuses to write under CI; D-022 boundary inclusivity pinned (exactly 300 m / exactly 30 min → duplicate); `ShelterPrefetchCount` constant named + assumption comment; `LatencyMs` XML doc; redelivery-grace comment. No contract changes, no new packages. Verification note: an external formatter had reflowed [docs/architecture/F1-blueprint.md](docs/architecture/F1-blueprint.md) and `DevRolePicker.razor` (whitespace/markdown-style only, `git diff -w` clean) — both restored from HEAD; the byte-exact Gemini goldens were checked and are undamaged (still compact single-line and blueprint-verbatim).

- **2026-09-02** — **F8 chunk 2 (real Gemini) implemented — F8 → DONE** (TDD: RED 40 failing → GREEN **275 passed + 1 live-skip** — 268 API incl. 34 new + 8 arch; build/publish 0 warnings): real `GeminiClient` (named "gemini" client, POST `v1beta/models/{model}:generateContent`, `x-goog-api-key` header — never URL/logs, D-026 per-request linked-CTS timeouts text/vision, zero retries incl. 429, non-2xx/timeout/network → `GeminiUnavailableException` metadata-only); `GeminiPromptBuilder` (verbatim systemInstruction + responseJsonSchema — not deprecated `responseSchema`, `<incident_description>` fencing w/ case-insensitive closing-tag escape, thinkingConfig MINIMAL/temp 0/max 256, compact JSON) pinned byte-exact by committed goldens (`Ai/Goldens/gemini-request-{text-only,with-photo}.json`, D-031) + independent verbatim asserts + no-PII assert (no location/id/timestamps); composite loads first photo per D-024 (`IFileStorage`, ext-derived MIME jpg/png/webp, unreadable/unknown/missing → text-only + log, extras dropped + count logged, D-030 client signature); parser confirmed against full realistic v1beta envelope (role/modelVersion/responseId/usageMetadata breakdown tolerated); composite regression matrix now also runs through the REAL client against a fake `HttpMessageHandler` (incl. key-never-in-logs assert); `LiveGeminiFactAttribute` (ctor-set Skip when `GEMINI_API_KEY` unset — skip verified) + one opt-in live flood-classification smoke; README AI data-flow & consent note. No contract/other-feature changes; no new packages.

- **2026-09-01** — **F8 chunk 1 (offline AI pipeline) implemented — F8 → IN PROGRESS** (TDD: RED 88 failing → GREEN **242/242** — 234 API incl. 92 new + 8 arch; build/publish 0 warnings; blueprint committed at [docs/architecture/F8-blueprint.md](docs/architecture/F8-blueprint.md)): `Features/Ai` grown per blueprint — `AiAssessment`/`AiDbContext` (`ai_assessments`, unique IncidentId idx, snapshot columns, `__efmigrationshistory_ai`, SQLite ticks gates on both date columns) + design-time `Initial` migration (Npgsql timestamptz; Sample/Auth migrations untouched); `PriorityFormula` extracted from `RuleBasedAiAnalysisService` (refactored onto it — outputs byte-identical, goldens pinned); feature-local `GeoMath` Haversine (§4.1 — no Stubs reference); D-021 pipeline (`AiWorkItem`, `IncidentCreatedHandler`, bounded DropWrite channel w/ drop-logging callback, `AiAnalysisWorker` w/ scope-per-item, idempotent redelivery skip + unique-index race catch, degraded analyze+publish-skip-persist per D-028); D-022 `DuplicateDetector` (seeded-pair analog links at ~130 m/20 min; 60-min/300-m/type/self/Resolved negatives all covered); Gemini scaffolding — internal `IGeminiClient` + chunk-1 placeholder throwing `GeminiUnavailableException` (real HTTP = chunk 2), D-025 `GeminiCircuitBreaker` (pinned-clock unit tests: threshold/window/half-open probe), `GeminiResponseParser` (closed enums case-sensitive, severity 1–5, STOP-only, summary truncate-200 + control-strip, confidence clamp log-only), D-028 `GeminiAiAnalysisService` composite (missing key → straight to rule-based w/o breaker count; breaker-open skips client; every failure mode in the matrix yields Provider="RuleBased" and never throws; telemetry ModelName/LatencyMs/TokensUsed/FinishReason captured); `/api/ai/assessments/{id}` (envelope, 404, 503 degraded gate) + `/api/ai/recommendations/shelter|team|resource` per D-027 (deterministic against seed data; full+closed shelters excluded; snapshot-first incident resolution) with `.RequireAuthorization()` + no-store filter + new "ai" rate-limit policy (Program.cs + `RateLimiting:Ai` 30/60); `AiModule` full D-028 wiring incl. structural `AddHttpClient("gemini")` slot + `MigrateAsync`; appsettings `Ai` section (empty ApiKey committed — key only via user-secrets/env); `TestingWebAppFactory` + `AiDbContext` lines; CI postgres-fidelity applies AiDbContext migrations. Factory SQLite rebind per D-029 fixed a pre-existing latent dispose-race flake (6 consecutive full-suite green runs). Runtime smoke (published, no DB, no key): degraded health, assessments → 503, recs unauthenticated → 401, shelter recs → 200 no-store with exact expected candidates. Zero contract changes; no new packages.
- **2026-09-01** — F1 post-review hardening (concurrency-safe rotation, headers, timing uniformity, image-only photos, JWT alg pinning): `RefreshToken.RevokedAtUtc` is now a concurrency token (+ empty `RefreshTokenConcurrency` migration for snapshot consistency — no Npgsql schema change) and a lost rotation race is treated as reuse (family revoke + `AuthEvent("TokenReuse","ConcurrentRotation")` → uniform 401); global `X-Content-Type-Options: nosniff` + `Cache-Control: no-store, private` on all `/api/auth/*` + `Content-Disposition: inline; filename=photo{ext}` on the photo stream; unknown-email login burns a dummy PBKDF2 verification (timing uniformity); photo upload restricted to image extensions at the endpoint (storage whitelist unchanged); JwtBearer pins `ValidAlgorithms=[HS256]` + `ValidTypes=["JWT"]`; register compensates a failed role-assign by deleting the created user; `MintSessionAsync` takes `IHostEnvironment` via DI (service-locator dropped), `BuildProfile` now sync; F1-blueprint de-duplicated (single spec copy matching §7); client catches widened to `TaskCanceledException` (AuthApi login/register + Profile page), `RedirectToLogin` uses `replace: true`, DevRolePicker deduped to one select; expired-refresh-row + parallel-refresh + concurrency-token + pdf-reject regression tests added — **150/150 tests green**, build/publish 0 warnings.
- **2026-09-01** — **F1 chunk B (client slice) implemented — F1 → DONE** (build+publish 0 warnings; **146/146** tests green — no server code changed; degraded runtime smoke passed): `Microsoft.AspNetCore.Components.Authorization` 8.0.30 (only new package); `Common/Auth` — `JwtAuthStateProvider` (in-memory token, manual Base64Url payload parse accepting nameid/sub + unique_name/email + role string-or-array per blueprint risk 6), `AuthMessageHandler` (same-origin Bearer, <60 s pre-emptive single-flight refresh via handler-free AuthApi, strips `X-Dev-Role` while signed in — real login wins), shared `HttpOrigin` guard (DevRoleHandler refactored onto it), `RedirectToLogin` (`/login?returnUrl=`), `LoginDisplay` (name + sign-out), `DevRolePicker` disabled+retitled while authenticated; `Features/Auth` — `AuthModels.cs` hand-mirrored wire DTOs (D-019), `AuthApi` (handler-free client for login/register/refresh/logout; refresh 401→ClearSession, 503/offline→keep state; logout sends Bearer explicitly then always clears), pages `/login` (returnUrl honor + open-redirect guard, 401/503 messages), `/register` (5 fields, ValidationProblem dict → per-field incl. Identity codes, auto-login), `/profile` (`[Authorize]`, edit/PUT, photo upload `.jpg/.jpeg/.png/.webp` ≤10 MiB via multipart, photo rendered as data URI through the main client — an `<img>` can't send Bearer); shell — `AuthorizeRouteView`+NotAuthorized in App.razor, `AddAuthorizationCore`/`AddCascadingAuthenticationState`/provider+AuthApi singletons + DevRole→AuthMessage→HttpClientHandler chain + boot-time silent `TryRefreshAsync` in Program.cs, LoginDisplay in MainLayout, Profile NavLink in AuthorizeView; CI AuthDbContext fidelity step verified pre-existing (chunk A). No browser-storage APIs anywhere (grep-proven); service worker untouched/asset-only. Client route guards key off real JWT sessions only — X-Dev-Role never unlocks client pages. Post-verification: open-redirect guard hardened (strict local-path allowlist `SafeDestination` — blocks `\` variants + all schemes; RedirectToLogin now emits `/`-prefixed returnUrl; logout catch widened to timeouts).
- **2026-09-01** — **F1 chunk A (server slice) implemented** — F1 → IN PROGRESS (TDD: RED 40 failing → GREEN **146/146** — 138 API incl. 52 new + 8 arch; build 0 warnings): `Microsoft.AspNetCore.Identity.EntityFrameworkCore` 8.0.30 (only new package); `Features/Auth` complete — `AppUser`/`RefreshToken` entities, `AuthDbContext` (base.OnModelCreating first, `auth_*` tables, `__efmigrationshistory_auth`, SQLite ticks gate on all 3 RefreshToken date columns) + design-time `Initial` migration (Npgsql timestamptz, Sample migrations untouched); `AuthModule` (AddIdentityCore+AddRoles+AddEntityFrameworkStores+AddSignInManager — never AddIdentity/AddDefaultTokenProviders; RequireUniqueEmail; `PasswordHasherOptions` from config; Npgsql gated ≠Testing; MigrateAsync+seeder); `TokenService` (JWT via default claim maps — principal identical to FakeAuth's; 32-byte Base64Url refresh, SHA-256 hex at rest, rotation inherits absolute expiry, reuse→family revoke+`AuthEvent("TokenReuse")`, stamp/lock checks, RevokeAll); 11 endpoints per blueprint B5 (uniform byte-identical 401s, `rr_refresh` cookie per D-012, IdentityResult→ValidationProblem, `.DisableAntiforgery()` on photo POST, self-lock/self-role 400 guards, 503 degraded gates, `RequireRateLimiting("auth")` on register/login/refresh — D-011 satisfied); `IdentityUserAdminService` displaces `FakeUserAdminService` via stub-yield (DI smoke pin updated to the displacement — blueprint ㉞); `AuthSeeder` (D-017 GUIDs all envs; 4 demo users Dev/Testing with FakeAuth GUIDs + `Demo!123`, idempotent, loud failures); `AuthSetup` ClockSkew 1 min; appsettings TTLs + `Auth:PasswordHasherIterations` (dev signing key already present from F0 hardening); `TestingWebAppFactory` upgraded (AuthDbContext SQLite + seeder + 64-char test key + 10k hasher iterations + temp `FileStorage:Root` + 64 KiB size cap for the oversize test); CI postgres-fidelity applies AuthDbContext. Blueprint committed at [docs/architecture/F1-blueprint.md](docs/architecture/F1-blueprint.md) — note: its body text drifts from the authoritative DECISIONS table in D-NNN numbering and role-GUID literals; the table wins (role GUIDs `aaaaaaaa-…`, D-017=role GUIDs, D-018=hasher iterations). No-DB smoke verified: `/health` degraded, login 503 problem+json, whoami+X-Dev-Role 200, `/sample` SPA 200, 11-rapid-logins→429. Next: chunk B (client pages + handlers + docs).

- **2026-09-01** — Post-review hardening batch (23 items from independent code + security reviews of F0; TDD, **94/94 tests green** incl. 17 new, build/publish 0 warnings with new `TreatWarningsAsErrors`): upload storage now rejects non-whitelisted extensions, enforces `FileStorage:MaxSizeBytes` (10 MiB default) during copy with partial-file cleanup, and derives ContentType server-side; paging clamps (page ≤1M, pageSize ≤200) in Ping GET + fake read services (kills int-overflow 500); JWT signing-key fail-fast outside Dev/Testing + "FAKE AUTH ACTIVE" startup banner + Production fail-closed 401 pin tests; `GET /api/sample/pings/{id}` (Location now resolves); `/api/**` 404 ProblemDetails fallback (never SPA HTML); RapidMap dispose-safe lifecycle (disposed flag, JSException guard, duplicate-id tolerance, `InitialCenter`/`InitialZoom` rename); event bus propagates cancellation; MigrationRunner cancel-safe; DevRolePicker renders `Roles.All` + DevRoleHandler same-origin guard; ping handler logs id/length never text; compose binds 127.0.0.1 + env-var healthcheck; CI `permissions: contents: read`; HSTS + HTTPS redirect outside Dev/Testing (D-010); opt-in forwarded headers (D-011); CSP meta + service-worker registration moved to `js/app-init.js`; Haversine sqrt clamp + Mirpur→Gulshan known-distance test; explicit `.env` ignore. No-DB smoke re-verified: degraded `/health`, `/sample` 200, `/api/nonexistent` 404 problem+json, whoami role flip, CSP + PWA assets served.
- **2026-09-01** — F0 chunk 3 implemented — **F0 DONE** (TDD, 77/77 tests green, build+publish 0 warnings): explicit `Microsoft.EntityFrameworkCore.Relational` 8.0.30 pin silences MSB3277; `DhakaSeedData` (28 deterministic incidents in the Dhaka bbox w/ fixed anchor 2026-09-01T12:00Z, 4 SOS, intentional near-duplicate pair `…05`/`…06` ~130 m/20 min apart, full severity+status coverage; 8 shelters incl. 1 full + 1 closed; 6 hospitals; 10 volunteers; 5 NGOs; 6 rescue teams data-only); `FakeIncidentReadService` (filters+paging, correct TotalCount), `FakeShelterReadService` (Haversine GetNearest), `FakeRegistryReadService`, `FakeUserAdminService` (4 FakeAuth-GUID users, in-memory mutate, false for unknown) behind `StubsModule` (Order=int.MaxValue, all `TryAdd*` — stub-yield proven by DI smoke test); `RuleBasedAiAnalysisService` + `AiModule` (keyword type match, bump words w/ ≤5 clamp, priority 20×sev + 25 SOS + 0–15 recency, clamp 0–100, Provider="RuleBased", D-009) and `NoOpRealtimeNotifier` + `RealtimeModule` in the real-service slots (plain `Add*`); `LocalDiskFileStorage` unit-tested (whitelist ext, hostile-filename discard, traversal-safe read/delete); Leaflet 1.9.4 vendored under `wwwroot/lib/leaflet` (no CDN) + `rapidMap.js` ES module (init/setView/upsertMarkers-diff/removeMarkers/click/dispose) + `RapidMap` Blazor wrapper (IAsyncDisposable, marker diffing) proven on `/sample` with seeded markers from new anonymous `GET /api/foundation/demo-incidents` (foundation-owned demo surface, remove-noted); `DevRoleState`/`DevRoleHandler`/`DevRolePicker` in MainLayout (SamplePage now uses the shared handler); `docs/api-conventions.md`, `docs/event-bus.md`, CODEOWNERS (placeholder handles), PR template. Runtime smoke (published artifact, no valid DB): degraded startup, health `degraded/dbConnected=false`, demo-incidents 200×28, ping POST 401 (no role) / 403 (Citizen) / 503 (Admin, degraded — 201 proven in SQLite tests), SPA + leaflet assets served. Team actions remaining: contract workshop ratification, 4/4 local runs.
- **2026-09-01** — F0 chunk 2 implemented (TDD, 30/30 tests green, no Postgres needed): `SampleDbContext` (table `sample_pings`, history table `__efmigrationshistory_sample`, provider-portable types, SQLite-gated `DateTimeOffset`→ticks conversion for test ordering) + design-time Initial migration (`--context`/`--output-dir` workflow proven); `MigrationRunner` (3×/2s retry per module, warn-and-continue-degraded, never crashes) + `DatabaseHealth` wired into Program step 16 and `/health` (`status`/`dbConnected`); full Ping slice per D-008 — `POST /api/sample/pings` (RequireAdmin, explicit FluentValidation, 201 + envelope + Location, publishes `PingCreated`, 503 ProblemDetails when degraded), `GET /api/sample/pings` (anonymous paged envelope, also 503-gated as DB-backed per D-005), `PingCreatedLoggingHandler`, Blazor `/sample` page (posts with `X-Dev-Role: Admin`, shows validation errors) + nav link; `TestingWebAppFactory` upgraded (per-context kept-open SQLite `:memory:` + EnsureCreated, scales one line per future context); docker-compose + `.env.example` per B7; CI `postgres-fidelity` job applies migrations against `postgres:16`; README run guide (compose primary, Neon/Supabase fallback, degraded mode, tests). No-DB smoke: degraded startup logged, `/health` degraded/dbConnected=false, ping POST/GET → 503, SPA + whoami fine. Next: chunk 3 (stubs + Dhaka seed + RapidMap + docs).
- **2026-09-01** — F0 chunk 1 implemented (TDD, 17/17 tests green): blueprint committed to docs/architecture/F0-blueprint.md; global.json + Directory.Build.props + solution with 3 src / 2 test projects; full Contracts v1 in `RapidRelief.Shared/Contracts` (B2, incl. `IEventBus` + `PingCreated`); scoped `InProcessEventBus` (D-006); `IFeatureModule` + reflection `ModuleDiscovery` (deterministic Order-then-Name sort); MultiAuth policy scheme (JwtBearer + SignalR access_token hook + FakeAuth for Dev/Testing with fixed seed GUIDs); role policies; Serilog + ProblemDetails + status-code pages + rate limiter (named auth/reports policies, skipped in Testing) + FluentValidation registration; `FoundationModule` (`/api/foundation/whoami` [Authorize], `/health`); hosted Blazor WASM PWA shell served by Api; `.config/dotnet-tools.json` (dotnet-ef 8.0.30); CI build-test workflow. Package pins exactly per blueprint B8. Next: chunk 2 (EF/Postgres pattern + Sample slice + SQLite test factory).
- **2026-09-01** — Repo created. Development plan + project context authored. No code yet. Next: F0 (Week 1).

## 9. How to Update This File

- After your PR merges to `main`: update your feature's **status row** (+ one-line note), add a **changelog line**, and append any new **decision** (D-NNN) — never rewrite history.
- Keep entries terse. Details belong in the plan, PRs, or code — this file is the _index of truth_, not the archive.
- If reality diverges from the plan (scope cut, owner swap, contract change), record it here **in the same PR** that makes the change.
